import type { Metadata } from "next";
import { Noto_Sans_JP, Roboto_Mono, Mohave, Syne } from "next/font/google";
import "./globals.scss";
import Header from "@/components/Header";
import Footer from "@/components/Footer";
import ScrollContainer from "@/components/scrollContainer";

const mohave = Mohave({
  subsets: ["latin"],
  variable: '--font-mohave'
});

const noto = Noto_Sans_JP({
  subsets: ["latin"],
});

const syne = Syne({
  subsets: ["latin"],
  variable: '--font-syne'
});

const roboto = Roboto_Mono({
  subsets: ["latin"],
  variable: '--font-roboto'
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
       <body
        className={`${noto.className} ${mohave.variable} ${syne.variable} ${roboto.variable} bg-[#EFFEF6]`}
      >
        <ScrollContainer>
          <div className="bg-[url(/assets/images/fv-bg.png)] bg-[size:100%_auto] bg-top bg-no-repeat">
            <Header />
            {children}
          </div>
          <Footer />
        </ScrollContainer>
      </body>
    </html>
  );
}
